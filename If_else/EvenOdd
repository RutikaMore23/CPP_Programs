#include<iostream>
using namespace std;

int main()
{
    int no;
    cout<<"Enter a number";
    cin>>no;
    if(no%2==0)
    {
        cout<<"Even Number";
    }
    else
    {
        cout<<"Odd Number";
    }
    return 0;
}
